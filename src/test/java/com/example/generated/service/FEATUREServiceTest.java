package com.example.generated.service;

import com.example.generated.dto.FEATUREDto;
import com.example.generated.entity.FEATURE;
import com.example.generated.repository.FEATURERepository;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;

import java.util.List;
import java.util.Optional;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.ArgumentMatchers.any;
import static org.mockito.Mockito.*;

/**
 * Unit tests for FEATUREService.
 *
 * Generated by MVAP TemplateCodeGenerator.
 */
@ExtendWith(MockitoExtension.class)
class FEATUREServiceTest {

    @Mock
    private FEATURERepository fEATURERepository;

    @InjectMocks
    private FEATUREService fEATUREService;

    private FEATURE testEntity;
    private FEATUREDto testDto;

    @BeforeEach
    void setUp() {
        testEntity = new FEATURE();
        testEntity.setId(1L);
        testEntity.setName("Test FEATURE");
        testEntity.setDescription("Test description");

        testDto = new FEATUREDto();
        testDto.setId(1L);
        testDto.setName("Test FEATURE");
        testDto.setDescription("Test description");
    }

    @Test
    @DisplayName("Should find all FEATURE records")
    void shouldFindAll() {
        when(fEATURERepository.findAll()).thenReturn(List.of(testEntity));

        List<FEATUREDto> result = fEATUREService.findAll();

        assertNotNull(result);
        assertEquals(1, result.size());
        assertEquals("Test FEATURE", result.get(0).getName());
        verify(fEATURERepository).findAll();
    }

    @Test
    @DisplayName("Should find FEATURE by ID")
    void shouldFindById() {
        when(fEATURERepository.findById(1L)).thenReturn(Optional.of(testEntity));

        Optional<FEATUREDto> result = fEATUREService.findById(1L);

        assertTrue(result.isPresent());
        assertEquals("Test FEATURE", result.get().getName());
        verify(fEATURERepository).findById(1L);
    }

    @Test
    @DisplayName("Should create new FEATURE")
    void shouldCreate() {
        when(fEATURERepository.save(any(FEATURE.class))).thenReturn(testEntity);

        FEATUREDto result = fEATUREService.create(testDto);

        assertNotNull(result);
        assertEquals("Test FEATURE", result.getName());
        verify(fEATURERepository).save(any(FEATURE.class));
    }

    @Test
    @DisplayName("Should delete FEATURE by ID")
    void shouldDelete() {
        doNothing().when(fEATURERepository).deleteById(1L);

        fEATUREService.delete(1L);

        verify(fEATURERepository).deleteById(1L);
    }
}
